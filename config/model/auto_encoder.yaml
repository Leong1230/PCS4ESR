
# Managed by Hydra

defaults:
  - base


# ckpt_path: '/localhome/zla247/data/output/Scannet/AutoEncoder/Narval_checkpoints/LeakyReLU_AutoEncoder_stage-1_voxel-0.1_max-dist-0.1_overfit-False_intake--1_encoder-blocks-pn-6_decoder-inverse_distance-8_latent_64-64_64_lr-0.001_Lambda-A6000Ada_run_1_epoch=49.ckpt' 

stage1_ckpt_path: None

ckpt_path: '/localhome/zla247/data/output/Scannet/AutoEncoder/Stage2_checkpoints/LeakyReLU_AutoEncoder_stage-2_voxel-0.1_max-dist-0.1_overfit-False_intake--1_encoder-blocks-pn-6_decoder-inverse_distance-8_latent_64-64_64_lr-0.001_Lambda-RTX6000Ada_run_1_219.ckpt' 

training_stage: 2 # 3 for test
recompute_udf: False

trainer:
  max_epochs: 700

optimizer:
    target_: torch.optim.Adam
    lr: 0.001
    decreased_by: 10

loss:
  norm_initialization: True # initialize latent code by norm distirbution
  loss_type: L1 # L1 or L2
  use_reg: True # whether to use regularization
  code_reg_lambda: 1e-4 # regularization lambda

lr_decay:
  decay_start_epoch: 350

network:
  module: AutoEncoder

  # m: 16 # 16 or 32 (U-Net hidden dimension)

  use_color: True
  use_normal: False
  use_xyz: False

  positional_encoding: Fourier # Triplane
  latent_dim: 64 #
  use_decoder: False

  prepare_epochs: 10

  encoder:
    design: local-pointnet-unet
    save_every_epoches: 500
    local_pointnet: True
    down_sampler: None # None, MaxPool, Conv
    pn_hidden_dim: 64
    pn_n_blocks: 6
    use_unet: True
    voxel_size_out: 0.1
    feature_in: voxel_features
    unet_blocks: [1, 2, 3, 4, 5, 6, 7]
    unet_block_reps: 2

  udf_decoder:
    decoder_type: ConvONet # ConvONet or CrossAttention or Concatenate
    interpolation_mode: inverse_distance # inverse_distance or max or trilinear
    activation: LeakyReLU # LeakyReLU or ReLU or Softplus or ShiftedSoftplus
    local_coords: True
    normalize_encoding: True
    k_neighbors: 8 # 1 for no interpolation

    input_dim: 3
    hidden_dim: 64
    num_hidden_layers_before_skip: 2
    num_hidden_layers_after_skip: 2

  seg_decoder:
    decoder_type: ConvONet
    interpolation_mode: inverse_distance # inverse_distance or max or trilinear
    activation: ReLU # LeakyReLU or ReLU or Softplus or ShiftedSoftplus
    local_coords: True
    normalize_encoding: True
    k_neighbors: 8 # 1 for no interpolation

    input_dim: 3
    feature_dim: 64
    hidden_dim: 64
    num_hidden_layers_before_skip: 2
    num_hidden_layers_after_skip: 2


dense_generator:
  num_steps: 10
  threshold: 0.1
  num_points: 900000
  filter_val: 0.005
  prepare_epochs: 900
  type: voxel # multiple_voxels or voxel or scene

inference:
  term: UDF # UDF or Seg
  split: val
  evaluate: True
  save_predictions: True
  visualization: False
  show_visualizations: True
  visualize_given_voxels: False
  visualize_ids: [406, 405, 366, 365, 403, 402, 410, 363, 409, 362]