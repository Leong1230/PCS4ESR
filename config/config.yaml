# Managed by Hydra

hydra:
  output_subdir: null
  run:
    dir: .

defaults:
  - _self_
  - data: base
  - model: base

# experiment_name: ${model.network.module}_Fixed-Unet-voxel-${data.voxel_size}_stage-${model.training_stage}_overfit-${data.over_fitting}_intake-${data.take}_${data.voxel_size}_latent-${model.network.latent_dim}_norm-initial_inner-steps-${model.optimizer.inner_loop_steps}_lr-${model.optimizer.lr}-${model.optimizer.latent_lr}-${model.optimizer.inner_loop_lr}_reg-lambda-${model.loss.code_reg_lambda}_${model.loss.loss_type}-blocks-${model.network.functa_blocks}_run_1

# experiment_name: ${model.network.module}_stage-${model.training_stage}_voxel-in-out-${data.voxel_size}-${model.network.encoder.voxel_size_out}_overfit-${data.over_fitting}_intake-${data.take}_${model.network.encoder.feature_in}_use-unet-${model.network.encoder.use_unet}_latent-${model.network.latent_dim}-${model.network.seg_decoder.feature_dim}_lr-${model.optimizer.lr}_${model.loss.loss_type}-blocks-${model.network.encoder.blocks}_run_1

#Auto-Encoder
# experiment_name: ${model.network.module}_stage-${model.training_stage}_voxel-in-out-${data.voxel_size}-${model.network.encoder.voxel_size_out}_overfit-${data.over_fitting}_intake-${data.take}_use-decoder-${model.network.use_decoder}_${model.network.encoder.feature_in}_encoder-${model.network.encoder.design}_${model.network.interpolation_mode}-${model.network.k_neighbors}-latent-${model.network.latent_dim}-${model.network.encoder.pn_hidden_dim}-${model.network.latent_dim}-${model.network.seg_decoder.feature_dim}_lr-${model.optimizer.lr}_ratio-sampling_debug

#Decoder-Comparsion
# experiment_name: Debug_stage-${model.training_stage}_voxel-${data.voxel_size}_overfit-${data.over_fitting}_intake-${data.take}_udf-queries-${data.udf_queries.ratio_per_epoch}-${data.udf_queries.queries_ratio_on_surface}__use-decoder-${model.network.use_decoder}-${model.network.udf_decoder.decoder_type}_local-coords-${model.network.udf_decoder.local_coords}_normalize-encoding-${model.network.udf_decoder.normalize_encoding}_${model.network.udf_decoder.interpolation_mode}-${model.network.udf_decoder.k_neighbors}-blocks-${model.network.udf_decoder.num_hidden_layers_before_skip}_latent-${model.network.encoder.pn_hidden_dim}-${model.network.latent_dim}-${model.network.udf_decoder.hidden_dim}_lr-${model.optimizer.lr}_run_1


# experiment_name: Auto_encoder_debug_1
#Vanilla-Minkowski
experiment_name: LeakyReLU_${model.network.module}_Fixed-Unet-voxel-${data.voxel_size}_overfit-${data.over_fitting}_intake-${data.take}_use-decoder-${model.network.use_decoder}_neighbors-${model.network.seg_decoder.k_neighbors}-blocks-${model.network.seg_decoder.num_hidden_layers_before_skip}_latent-${model.network.latent_dim}-${model.network.seg_decoder.hidden_dim}_lr-${model.optimizer.lr}_test
# experiment_name: debug
project_root_path: ${hydra:runtime.cwd}
exp_output_root_path: ${project_root_path}/../data/output/${data.dataset}/${model.network.module}/${experiment_name}

global_train_seed: 123
global_test_seed: 321